name: 'sql_for_data_platforms'
version: '1.0.0'
config-version: 2

profile: 'sql_for_data_platforms'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sql_for_data_platforms:
    staging:
      +materialized: view
    marts:
      +materialized: table
